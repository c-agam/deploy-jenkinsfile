node {
    stage('Checkout') {
        git branch: 'master' ,credentialsId: '....', url: 'https://zul@gtr.qmcai.com/qmbd/mgr.git'
    }
    stage('Maven Build') {
        sh 'mvn clean package -Ptest -Dmaven.test.skip=true'
    }
    stage('Build Image') {
        withDockerRegisty()
    }
    stage('Push Image') {
        echo 'Push Image'
    }
}